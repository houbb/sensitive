# 目标

## 问题1

```
如何扩展脱敏策略，比如内置的中文脱敏只能支持2-3个中文，需要支持新疆名字该如何操作
```

## 问题 2

```
如何脱敏支持非0开头的手机号，比如00开头
```

# 要求

看一下这些问题的共性，实际上都是向自己自定义策略。

看一下我们的注解、核心、以及日志插件。

看一下目前的支持情况，以及如果想支持，需要修改哪些代码。最少修改+最佳拓展性。

## 补充信息

D:\github\chars-scan 源码

### 测试

先判断下 jdk 文件是否存在，看一下选择哪一个？

```
$env:JAVA_HOME="D:\tool\jdk\jdk-1.8"; mvn test
```

or:

```
$env:JAVA_HOME="D:\tools\jdk\jdk1.8.0_192"; mvn test
```

# 沟通

## 1

不需要修改以前的逻辑  只需要在配置文件中，添加内置策略的自定义支持即可。你看下以前的功能应该已经基本实现 了，但是有一些问题需要修正

D:\github\chars-scan 源码，这是另外一个项目，支持拓展，但是不应该修改其中的任何代码。

我们只需要在当前项目，配置文件中允许用户自定义策略。默认用内置的
