# 目标

## 原始需求

```
每次打印日志都带密钥，这个有配置可以关掉吗
{"phoneNumber":"155****5678|ECA0459CC10A791F4074E06240373F86"}
```

## 大概思路

给用户一个配置项，可以方便的决定是否开启密钥打印。

## 要求

我们需要统一注解+core+日志插件。

提供一个三者通用的使用方式，方便用户自定义配置。

## 要求

首先梳理请求，确认后，在实现。

多种方案对比，给出最佳方案+原因。

## 新版本

新的版本使用 version 1.8.0

更新对应的 CHANGE_LOG.md + README.md


# 讨论1

showHashInLog 改为 showHash 就行。简单一些。  

可以，使用推荐方案，注意优先级，一定要说清楚。要方便配置可以指定+覆盖。

最好可以在日志中指定我们的配置路径。

然后将以前的日志中的配置，也提取到配置文件中，以前的日志文件尽可能的简洁优雅

# 讨论2

这种优先级覆盖，能不能有一个地方输出一下真实的配置，方便用户排查？！  

另外，你梳理一下整体，确保你的配置修改影响面最少+拓展性最佳。